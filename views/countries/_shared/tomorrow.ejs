<%
  const labels = country?.labels || {};
%>
<section class="section-card hero-card">
  <div class="hero-meta">
    <span class="eyebrow">Plan ahead for <%= country?.name || 'Africa' %></span>
    <span class="pill-outline">Tomorrow tips</span>
  </div>
  <h1 class="hero-title"><%= labels.dayTomorrow || 'Tomorrow' %> Tips</h1>
  <p class="hero-subtitle">Get early picks for the next match day.</p>
</section>

<section class="section-card">
  <div class="section-head">
    <h2 class="section-title"><%= labels.dayTomorrow || 'Tomorrow' %> Tips</h2>
    <p class="section-subtitle">Updated daily based on form, injuries and match context.</p>
  </div>
  <%- include('../../partials/tips/tip-cards', { tips: tips?.tomorrow || [] }) %>
</section>
